const config = {
	webServer: {
		// Use this for dev:
		// command: 'npm run dev',
		// port: 5173,
		// reuseExistingServer: true

		// this for CI/Prod
		command: 'npm run preview',
		port: 4173
	}
}

export default config
