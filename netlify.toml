[build]
command = "npm run generate"
publish = "dist"
# functions = "dist/functions/"

# [dev]
#  command = " dev"
# targetPort = 5173

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"
